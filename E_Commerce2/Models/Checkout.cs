using System.ComponentModel.DataAnnotations;

namespace E_Commerce2.Models
{
    public class Checkout
    {
        public bool ProcessOrder(Order order, decimal amount)
        {
            // Simplified logic to process orders
            return true; // Always assume successful payment for the portfolio project
        }
    }
}
